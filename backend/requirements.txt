# Elevator Configuration & Pricing Engine — Python Dependencies
#
# Install with:
#   python -m pip install -r requirements.txt
#
# Python 3.12+ required.

# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# ── Data Validation & Settings ────────────────────────────────────────────────
pydantic>=2.9.0
pydantic-settings>=2.6.0

# ── HTTP Client (used by tests and future integrations) ───────────────────────
httpx>=0.28.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.3.0
pytest-asyncio>=0.24.0

# ── Code Quality ──────────────────────────────────────────────────────────────
ruff>=0.8.0
black>=24.10.0
pre-commit>=4.0.0
